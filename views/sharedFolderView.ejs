<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Shared Folder</title>
    <link rel="stylesheet" href="/styles.css">
</head>
<body>
    <div class="container">
        <h1>Shared Folder: <%= folder.name %></h1>
        <p>Files in this folder:</p>
        <ul>
            <% files.forEach(file => { %>
                <li>
                    <a href="<%= file.publicUrl %>" download><%= file.filename %></a>
                </li>
            <% }) %>
        </ul>
    </div>
</body>
</html>
